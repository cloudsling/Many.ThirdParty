<Page
    x:Class="Many.ThirdParty.MainFrameContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Many.ThirdParty"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"
              Background="Red"
              MinHeight="40">

        </Grid>
        <Grid Grid.Row="1">
            <Frame />
        </Grid>
        <StackPanel x:Name="NavBtn"
                    Grid.Row="2"
                    Background="Red"
                    MinHeight="40">
        </StackPanel>
    </Grid>-->
    <RelativePanel Background="LightGray">
        <Grid x:Name="topGrid" Grid.Row="0"
              Background="Red"
              Width="500"
              MinHeight="40"
              RelativePanel.AlignTopWithPanel="True">

        </Grid>
        <Grid Grid.Row="1" RelativePanel.Below="topGrid"
              Width="500"
              RelativePanel.Above="NavBtn">
            <Frame x:Name="mainFrame"/>
        </Grid>
        <StackPanel x:Name="NavBtn"
                    Grid.Row="2"
                    Background="Red"
                    MinHeight="40"
                    RelativePanel.AlignBottomWithPanel="True">
        </StackPanel>
    </RelativePanel>
    <!--<VisualStateManager.VisualStateGroups x:Name="SizeC">
        <VisualStateGroup>
            <VisualState>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>-->
</Page>
